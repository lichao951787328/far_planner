# FAR 未知区域动态避障测试记录（2026-08-08）

## 正式测试结论

测试结果：**PASS**。

- 原始目标：`(-16.0, 14.0, 0.291)`，只发布一次。
- 初始距离：`14.976 m`。
- 初始目标地图状态：`unknown`。
- 机器人运动后目标区域变为已知，原始目标坐标未改变。
- 检测到 2 次动态障碍进入规划走廊，共 15 个监测采样。
- 第一次进入走廊后约 `0.6 s`，局部路径航向开始变化。
- waypoint 共变化 8 次；动态障碍离开后恢复为原始目标，无需重新发布目标。
- 机器人与动态点云最近距离：`0.842 m`，大于 `0.6 m` 演示安全距离。
- 最大线速度：`0.275 m/s`，低于 `0.3 m/s` 限速。
- 最大横滚/俯仰：`6.521° / 7.746°`，姿态正常。
- `reached=true` 时目标误差：`0.311 m`，小于 `0.5 m`。
- 到达后 `/far_cmd_vel` 与 `/quad_cmd_vel` 均为零，间隔约 2.6 秒的两次位姿基本不变。

原始自动验收文件：

- CSV：`/tmp/leader_unknown_dynamic_west_20260808.csv`
- 汇总：`/tmp/leader_unknown_dynamic_west_20260808_summary.txt`

## 分阶段证据

### 1. 初始 unknown

- 重新启动 Gazebo 与语义建图，清空旧 OctoMap 和 Graph。
- 机器人初始位置约为 `(-1.024, 14.000)`。
- 目标距离为 `14.976 m`，监测器直接查询八叉树得到 `unknown`。
- `far_reach_goal_status=false`。

### 2. 只规划、不运动

- 仅启动 FAR 和 localPlanner，不启动 pathFollower。
- FAR waypoint 为 `(-16, 14)`，局部 `/path` 为 101 点。
- `/far_cmd_vel` 和 `/quad_cmd_vel` 均无发布者，机器人保持静止。

### 3. 低速进入未知区域

- pathFollower 在 `/stop=2` 下启动；确认两路速度均为零后，由安全守护器发布 `/stop=0`。
- 目标区域随运动从 `unknown` 变为已知。
- 目标距离持续从 `14.976 m` 降低。
- 实际最大线速度为 `0.275 m/s`。

### 4. 动态障碍响应

- 第一次动态走廊事件发生在记录时间 `121.858 s`。
- `/path` 航向在 `122.458 s` 开始偏转，响应时间约 `0.6 s`。
- waypoint 在 `124.458 s` 开始切换到绕行节点。
- 第二次走廊事件发生在 `126.258 s`，waypoint 在 `126.858 s` 再次变化。
- 机器人没有穿过动态点云，实际最近距离为 `0.842 m`。

### 5. 障碍离开后恢复

- 第二次走廊事件于 `131.258 s` 结束。
- waypoint 恢复为原始目标 `(-16, 14)`，未重新发布目标。
- 机器人继续前进，没有长期停车或在节点之间持续振荡。

### 6. 最终到达

- 首次记录到 `reached=true` 时目标误差约 `0.311 m`。
- 全程 `goal_message_count=1`、`goal_change_count=0`、`false_reach=no`。
- 安全守护器在正确到达后发布停车指令；最终速度为零且位姿稳定。

## 未采用的预试验

目标 `(12, -14)` 的预试验也证明了初始 `unknown` 和目标不变，但机器人推进约 6.4 m 后，FAR 图中没有可连接目标的边，长期输出单点停车路径。动态物体当时距机器人约 2.9 m，因此不能将该停车解释成动态避障。该轮结果保留为失败诊断，不用于汇报成功视频。
