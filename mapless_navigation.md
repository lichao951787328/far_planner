<!--
 * @Author: error: error: git config user.name & please set dead value or install git && error: git config user.email & please set dead value or install git & please set dead value or install git
 * @Date: 2026-07-23 11:15:48
 * @LastEditors: lichao951787328 951787328@qq.com
 * @LastEditTime: 2026-08-07 17:15:37
 * @FilePath: /far_planner/mapless_navigation.md
 * @Description: 这是默认设置,请设置`customMade`, 打开koroFileHeader查看配置 进行设置: https://github.com/OBKoro1/koro1FileHeader/wiki/%E9%85%8D%E7%BD%AE
-->
graph可以保留，全地图（自由、障碍、可通行）给我的高程图我还当作全局地图用吗？还是只当局部地图用，全局地图还是依旧保留这直接使用点云的方案，累积误差影响大吗？地图端会维护一个全局的点云和语义地图吗？除了全局地图需要维护几何信息之外，建议维护一个和高程相同维度的代价地图，这个代价地图统一把语义代价和可通行性代价揉在一起，注意代价的卡尔曼更新与动态物体的去除。waypoint可以结合之前的方案，然后根据布局代价来确定，这个确定可能会有一些工作量。
在确定waypoint之后，使用带代价的地图进行规划路径，这里在规划时可以考虑梯度的方向及各种代价。可以先写一个保底的，后面可以使用自监督，我觉得这个来做自监督应该可以。


后续直接通过局部地图来和维护动态障碍，全局地图依然保留
通过局部障碍地图来graph是不是就可以，能区分动态障碍就行，现在有点像把全局地图当局部地图用了




里程计回环与代价地图累积的代价地图更新，可以使用一个代价地图就完成障碍+代价的维护吗？不考虑别的区域。如果有回环，可通行代价和语义代价可以怎么维护。


先不考虑里程计的误差+自己维护代价地图+传统的局部规划器。


把 Util/dyosb_update_thred 从 4 降到 2（先试）
把 Util/dynamic_obs_dacay_time 从 10 降到 3-5
把 CDetector/filter_count_value 从 3 提到 4 或 5（减少稀疏伪轮廓）
若仍有“厚边”，再评估 Util/obs_inflate_size 是否过于保守

1、在不考虑语义区域渐变式自适应可通行区域的前提下
重心1：维护一个具有切实指导意义的graph，也就是要维护一个全局的二维概率栅格地图。
如果上层直接给语义分割障碍区域和动态障碍，确定是动态障碍的才当作动态障碍，例如人、狗等移动生命活体；把建筑等固定的当作确定性的静态障碍；把车当作初步静态障碍，在传输点云时，要筛选出非动态点云的物体，然后维护这个全局的二维概率栅格地图。

重心2：
二维栅格地图直接转img、进行过滤操作之后即可进行维护graph，graph可能需要不会整体性的维护，只维护局部，然后再拼成全局。

2、局部规划层面，依靠地形高程图，叠加当前的全部障碍和地形可通行性代价，进行规划。或者自行构建一个随着机器人坐标更新的https://voxblox.readthedocs.io/en/latest/index.html,https://github.com/ethz-asl/voxblox.git

Voxblox 坐标系：固定不动（与世界坐标系平行）。
滚动窗口：仅指空间位置随机器人平移，不包含旋转。
对规划的影响：无负面影响。规划器通过 tf 变换将世界坐标系的障碍物转换到机器人坐标系下进行计算。
性能优势：这种设计避免了昂贵的网格旋转重采样，保证了实时性。

实现思路：非动态点云加在到octreemap，维护一个全局暂静态的三维地图，当作全局地图使用。同时，截取机器人附近区域部分，添加上动态障碍，并添加上可通行性代价和障碍。

第二步：octreemap在转二维图维护障碍地图时，如果地面是三维场景，是不是应该考虑梯度场，


第三步：给每个栅格补上语义标签参考ssmi@github

全局地图回环层面：
策略一：增量式更新 + 概率衰减（最常用，OctoMap 标准做法）
这是大多数实时系统（如使用 octomap_mapping ROS 包）的做法。它不主动擦除旧错误，而是依靠后续的新数据来慢慢“覆盖”或“纠正”错误。

1. 原理：贝叶斯更新（Bayesian Update）
八叉树的每个节点存储的是一个**对数几率（Log-Odds）**值 l ，表示该体素被占据的概率。

当传感器射线穿过某个体素时，如果检测到障碍物，增加 l ；
如果射线畅通无阻（Free Space），减小 l 。
2. 回环发生后的过程
位姿跳变：后端优化器计算出新的位姿图，当前机器人的位姿突然修正到了正确位置。
新数据插入：机器人继续运动，新的激光雷达/深度相机数据基于修正后的新位姿插入到八叉树中。
自然纠正：
假设之前因为漂移，机器人在 A 处误以为前方有墙（标记为占据）。
回环后，机器人实际上在 B 处，它看向原来的“墙”的位置，发现那里是空的（射线穿透）。
新的射线会穿过那个原本被标记为“占据”的体素。
根据贝叶斯更新规则，每次射线穿过，该体素的占据概率 l 就会下降。
经过多次观测后，该体素的概率会降到阈值以下，从“占据”变为“空闲”。
✅ 优点
实时性高，只需处理当前帧数据。
实现简单，OctoMap 库原生支持。
❌ 缺点
收敛慢：需要机器人再次经过该区域或从其他角度观测，才能消除错误。
残留伪影：如果机器人不再经过错误区域，那些错误的“鬼影墙”会一直存在。

策略二：局部子图重建（Local Submap Reconstruction）
为了加快纠正速度，一些高级 SLAM 系统（如 LIO-SAM, FAST-LIO2 配合特定地图管理器）会采用局部窗口机制。
1. 原理
不维护一个巨大的全局静态八叉树，而是维护一个滑动窗口内的局部高精度地图（可能是点云或局部体素网格）。
全局地图只保留稀疏的关键帧或低分辨率概览。
2. 回环发生后的过程
位姿修正：后端优化调整了最近 N 个关键帧的位姿。
局部重投影：系统只取这 N 个关键帧对应的原始点云数据，用新的位姿重新投影，生成一个新的局部体素网格或八叉树。
合并到全局：将这个修正后的局部地图合并到全局地图中。由于只涉及最近的数据，计算量可控。
✅ 优点
能快速消除近期的回环误差导致的地图畸变。
比全量回溯快得多。
❌ 缺点
实现复杂，需要管理关键帧和局部地图的生命周期。
对于很久以前的错误（长期漂移），依然难以自动修正，除非再次形成回环。

策略三：离线全局地图优化（Offline Global Map Optimization）
如果你需要一张完美无缺的地图（例如用于数字孪生或高精地图制作），则不能在实时阶段完成，而必须在事后进行。

1. 原理
实时阶段只记录原始传感器数据（Raw Point Clouds）和时间戳。
位姿图优化完成后，得到了一组全局一致的位姿 {T } 。
1. 实现步骤
数据回放：读取所有历史帧的点云数据。
全局重投影：使用优化后的位姿 T，将每一帧点云转换到全局坐标系。
构建新地图：
将所有重投影后的点云合并。
进行一次性的**体素滤波（Voxel Grid Filtering）**去噪。
或者，将这些点云一次性插入到一个新的八叉树中。
冲突解决：由于位姿已经全局一致，不同帧对同一位置的观测会自然重合，不会产生重影。如果有少量噪声，可以通过统计离群点去除（Statistical Outlier Removal）。
✅ 优点
地图质量最高，无鬼影，无漂移痕迹。
可以生成高分辨率的 Mesh 或精细八叉树。
❌ 缺点
非实时：计算量大，可能需要几分钟到几小时。
不适合导航：只能用于后处理展示或存档。
增加GPU并行计算

